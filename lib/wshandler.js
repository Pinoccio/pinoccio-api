var rpcStream = require('rpc-stream');
var mdm = require('mux-demux');

module.exports = function(standardHandler){
    var handler = function(ws){
      // call standard handler for each message.
      // handle streams etc.
    }
}
